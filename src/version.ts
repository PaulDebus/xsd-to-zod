// The running package version. Generated files are stamped with it so the
// runtime can warn when a file predates a breaking change; the version-sync
// test keeps it in lockstep with package.json.
export const PACKAGE_VERSION = "0.3.1";
